[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "entrax"
version = "1.0.0"
description = "A Python toolkit and CLI for processing PCAP traffic captures, generating flow/feature datasets, and running analysis workflows. It includes utilities for dataset inspection and classic tree-based models (CART, Random Forest, Boosting) used in traffic classification experiments."

requires-python = ">=3.8"
dependencies = [
    "pytest",
    "numpy",
    "pandas",
    "scikit-learn",
    "pyshark",
    "matplotlib",
    "seaborn",
    "mypy",
    "pandas-stubs",
    "types-seaborn",
    "argcomplete",
    "graphviz",
    "scipy",
    "scipy-stubs",
]

[tool.setuptools]
packages = ["entrax", "entrax.utils", "entrax.pcap_processing", "entrax.tree_models"]

[project.scripts]
entrax = "entrax.cli:main"